name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: x86_64-apple-darwin
            os: macos-latest
            name: cmdvault-darwin-amd64
          - target: aarch64-apple-darwin
            os: macos-latest
            name: cmdvault-darwin-arm64
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            name: cmdvault-linux-amd64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename binary
        run: mv target/${{ matrix.target }}/release/cmdvault ${{ matrix.name }}

      - name: Package binary
        run: |
          tar -czf ${{ matrix.name }}.tar.gz ${{ matrix.name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: ${{ matrix.name }}.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            cmdvault-darwin-amd64/cmdvault-darwin-amd64.tar.gz
            cmdvault-darwin-arm64/cmdvault-darwin-arm64.tar.gz
            cmdvault-linux-amd64/cmdvault-linux-amd64.tar.gz
          generate_release_notes: true
